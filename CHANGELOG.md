# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- 书架新增内置「隐藏」分组：把书移入后，该书不再出现在「全部」、来源筛选、自定义分组与书架搜索里，只能通过筛选条里的「隐藏」tag 查看（该 tag 排在各自定义分组之前）。
- 书架顶部选中的筛选标签（全部 / 来源 / 分组）会被记住，下次进入软件直接停留在该标签。
- 左右翻页模式下，阅读菜单底部按钮上方新增悬浮的章节页进度条：可点按 / 拖动跳转到本章任意一页，拖动中显示「x/y页」提示（听书悬浮球保持在进度条上方）；进度条上的「逐页刻度」（按页数等分的灰色圆点）与进度条本身都可在阅读设置中开启 / 关闭。
- 所有需要纵向滚动的页面 / 弹层（整页滚动区、阅读器目录/书签/全书搜索、听书与阅读设置、分组、WebDAV、许可文本等）接入移动端风格的自定义滚动条：内容可滚动时右侧浮现细滚动条，静止后自动淡出，可按住轨道或拖动拇指快速定位（拖动中拇指放大）。
- 发现页点击搜索结果 / 发现列表的书籍后改为呼出底部「书籍详情」抽屉（不切路由，保留发现页搜索词、结果与滚动状态）；抽屉内自动拉取书源「详情」与「目录」，可直接预览简介、章节列表，并选择「加入书架」或「加入书架并阅读」。
- 书源正文支持图片（图片正文）：`bookContent` 返回含 `<img>` 的 HTML 或对象 `{ text?, images? }` 即可在正文中展示插图 / 整章图片（漫画、扫描、图文混排）；图片经该书源 HTTP 会话（Cookie / 默认头 / UA，Referer 取正文页地址）下载为 data URL 随章节本地缓存，离线可读、对防盗链图站开箱即用。老书源纯文本返回与已缓存章节不受影响，书源 schema 与书籍数据格式均未变。
- 在线阅读窗口拉取改为「当前章优先、逐章落盘」，含图章节打开时无需等整窗下载完。
- 跳转书籍页数时显示**返回原进度**。
- 书架页面全选。
- 在线书「检查书籍更新」：阅读设置新增入口，点击重新获取书源目录并与书架目录比对——末尾新增章节自动追加并提示更新章节数；目录结构不一致（中段被新增 / 删除 / 重排或章节地址变动，无法安全追加）时弹窗询问是否整本覆盖更新，覆盖会保留章节地址未变章节的已缓存正文，其余按需重新获取。
- 书源导入确认页会列出与本机重复（同名 · 同站点）的书源，默认用导入内容覆盖，可逐条关掉开关以保留本机版本；顶部计数与完成提示同步扣除被保留的条目。
- 在线书「重新加载本章」改为先拉取新正文、成功后才覆盖落盘：正文变化可能导致本章书签失效时，先弹窗询问是否继续，取消则不改动正文与书签；拉取失败也保留本章原有正文（原实现会先清空缓存，失败后需重新获取）。

### Fixed

- 修复在线书（尤其含图片/大书）逐批下载正文时的明显卡顿与批量下载闪退：下载不再对整本做深拷贝、不再把整本 JSON（含 data URL 图片）反复经 IPC 传送，改为「只回写本次变动的章节」增量落盘（新增 Rust command），并在章节解析等耗时步骤之间让出主线程；阅读器在收到与当前章无关的正文回写时不再重置页码 / 重排当前章 / 闪「正在加载」。
- 发现页重选分类 / 书源标签后，旧列表不再静默挂着，而是先清空并显示「加载中…」占位，等新列表返回后再替换。
- TTS 自动跨章时，已取消「跟读跟随」的用户不再被强行切到新章节：朗读只在内部继续推进，阅读视图留在手动位置（点「返回跟读」仍可追回朗读所在章）。
- 分页模式跨页选取文本时，选区端需在页缘外**持续停留判定时长**才会翻页续选，且每次拖出只翻一页、回页内重新拖出才再翻，避免短暂越界便连续跨过多个页面。
- 分页模式跨页连选自动翻到新页后，起点 / 终点不在本页的那端不再显示手柄（此前会额外画一个贴页界的假手柄），本页只保留仍在页内的拖动端手柄。
- 阻止新增与已有书签部分重叠的书签：所选文字若与已有书签区间相交（选中了部分已书签文字、或选区横跨已书签与未书签文字），「书签」操作不再静默叠加重叠下划线，而是提示并保留原书签；仅当再次选中与书签完全相同的文字时才移除该书签。

### Changed

- 书签页面优化：使用卡片分隔开不同章节的书签。

## [0.1.2] - 2026-09-05

### Added

- 书源「网页登录」：Android 端在应用内 WebView 浮层登录后自动捕获站点 Cookie（含 httpOnly）并按书源持久化（重启自动注入，不随书源 JSON 导出）；书源 JS 引擎开放 `webview.login(url)` / `webview.isSupported()` 宿主 API，书源编辑页提供「网页登录 / 清空登录 Cookie」入口。桌面/iOS 不支持时返回 `ok:false` 且不抛错。
- 导入本地同名文件时可选择重新导入（替换书架中同名书，尝试继承阅读进度/分组/书签）或作为新书加入；重新导入前预演书签继承，部分书签无法继承时提示并可放弃重新导入。WebDAV 长按重新导入同样增加该提示与放弃选项。
- 书籍详情页。
- 目录（TOC）显示章节下载状态。
- 阅读设置里新增「重新加载本章」（仅在线书显示该入口）。
- 在线书获取失败后提示用户并提供重新加载入口。
- 替换文本功能，支持正则。

### Fixed

- webdav中直接点击阅读，返回后，失去状态数据的问题。
- tts跟读后翻页异常问题。
- 自动跟读跨章定位异常的问题。
- 朗读已进入下一章时点击「返回跟读」无法跳回朗读句的问题（未带上朗读所在章节索引）。
- 书签按钮异常高亮的问题。

### Changed

- TTS失败后跳过，连续失败多次（5次）后停止。
- 封面中，使用书籍全名，而不是首字。
- 封面可以在epub中取。
- 当章节内容过多时，阅读器显示正在加载，而不是卡页面。
- 阅读器设置继承设置页面的某些设置项。
- 当书籍有至少一个书签时，不填充svg图标。
- 跨页面选取支持（分页模式）。
- 书签跨段落支持。
- 将书本来源筛选与分组合并，并可以在设置调整。
- 设置页面中**听书缓存**移入到**数据**中。
- 分组配置在**书架**页面长按分组标签即可进入。

### Removed

- 删除了设置页面的**书架分组**栏目。

## [0.1.1] - 2026-09-04

### Added

- 本地书导入
- 添加tts听书功能
- 搜索全书，书架书籍功能
- webdav支持
- 书源

